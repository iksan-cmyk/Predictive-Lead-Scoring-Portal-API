# Server Configuration
HOST=0.0.0.0
PORT=3000
NODE_ENV=development

# Database Configuration (PostgreSQL)
PGHOST=localhost
PGPORT=5432
PGDATABASE=lead_scoring_db
PGUSER=postgres
PGPASSWORD=postgres

# JWT Configuration
ACCESS_TOKEN_KEY=your-super-secret-access-token-key-change-in-production
REFRESH_TOKEN_KEY=your-super-secret-refresh-token-key-change-in-production
JWT_ACCESS_TOKEN_EXPIRY=15m
JWT_REFRESH_TOKEN_EXPIRY=7d

# Bcrypt Configuration
BCRYPT_SALT_ROUNDS=10

# CORS Configuration
CORS_ORIGIN=*

# Model Configuration
MODEL_PATH=./src/model/model.onnx
FEATURE_SCHEMA_VERSION=1.0.0

# Redis Configuration (Optional - for caching)
REDIS_URL=redis://localhost:6379
CACHE_TTL=600